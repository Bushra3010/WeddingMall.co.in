<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      `usesCleartextTraffic` is deliberately not declared. Android already
      defaults it to false for targetSdk 28+, so setting it added nothing in
      production — and hardcoding false collided with the value Capacitor
      injects when `cleartext: true` points the app at a local dev server,
      failing the manifest merge outright.
    -->
    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              App Links, so a weddingmall.co.in link opens in the app rather
              than the browser. `autoVerify` needs the matching
              /.well-known/assetlinks.json on the site before Android will
              honour it silently; until that file is published the link simply
              opens in the browser, which is the safe failure.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="weddingmall.co.in" />
                <data android:scheme="https" android:host="www.weddingmall.co.in" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Vendor uploads. A `<input type="file">` inside the WebView is handled by
      Capacitor's bridge, but the chooser can only offer the camera and the
      gallery if the app declares them.

      READ_MEDIA_IMAGES/VIDEO are the Android 13+ (API 33) permissions;
      READ_EXTERNAL_STORAGE covers API 32 and below and is capped with
      `maxSdkVersion` so newer devices are not asked for a permission that no
      longer exists. WRITE_EXTERNAL_STORAGE is deliberately absent — uploading
      never needs it, and asking for it is the kind of over-request that gets
      an app rejected.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- The camera is for picking a photo, not a reason to exclude devices
         without one (tablets, some emulators). -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
</manifest>
